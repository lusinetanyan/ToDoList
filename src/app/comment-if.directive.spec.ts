import { CommentIfDirective } from './comment-if.directive';

describe('CommentIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentIfDirective();
    expect(directive).toBeTruthy();
  });
});
